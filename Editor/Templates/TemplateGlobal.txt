using LumosLib;

public class Global : BaseGlobal
{
      //MEMO : expanded in project
      /* 
      public static PlaySceneManager PlayScene
      {
            get => _ui ??= Get<PlaySceneManager>();
            set => _ui = value;
      }
      private static PlaySceneManager _playScene;
      */
}